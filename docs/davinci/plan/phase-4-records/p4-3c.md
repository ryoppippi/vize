# P4-3c — Unused setup bindings

Checked 2026-09-26 with the real CLI, after verifying the P4-6a diagnostic
law start gate in the implementation and its task record.

## Producer and consumer

`UnusedBindings` is the seventh primary Croquis group. Its name key maps
to an exclusive declaration byte span in the `Bindings` script frame;
stratum 1 declares a dependency on `Bindings`. The producer only projects
authoritative `Croquis.unused_bindings` storage.

`SfcCroquisOptions::with_unused_bindings()` requests population from the
already parsed script AST, using OXC symbol/reference resolution. Reads
in closures, `defineExpose`, JSX and type queries count; unrelated shadows
and writes alone do not. The existing template walk removes actual reads,
component import aliases, namespace tags and custom directives. The shared
CSS scanner supplies style `v-bind()` reads. Invalid syntax, direct eval,
external blocks and unsupported dialect views cannot prove non-use.
An inline template without its supplied AST also cannot prove non-use;
the descriptor API witness checks missing-view, supplied-view and script-only cases.

Pug lint passes explicit derived-template provenance while preserving the
authored descriptor language and source map. Ordinary compiler options
leave population disabled. No new script parse or unconditional template
walk is added to the compiler route; no performance improvement is claimed.

`vue/no-unused-setup-bindings` lives in `rules/facts/`, is opt-in, Warning,
and tier Sound. Its declared demand is `UnusedBindings`; underscore names
are conventionally exempt. LintContext shares one fact manager per artifact
with `vue/no-undefined-refs`. Script-only SFCs use one descriptor draw.

## Proof

| Plane       | Class (b) recall | Mapped baseline diagnostics | Baseline misses / unmappable / unexpected |
| ----------- | ---------------: | --------------------------: | ----------------------------------------- |
| P0-13 shard |          130/130 |                         701 | 0 / 0 / 0                                 |
| Matrix      |            90/90 |                         278 | 0 / 0 / 0                                 |
| Miniature   |              4/4 |                           2 | 0 / 0 / 0                                 |

The Rust command and MJS mirror use `davinciSeededUnused`, independently
mutate class (b), explicitly enable the rule, and compare full diagnostic
multisets with exact rule, severity and span identities. A wrong rule at the
correct location is rejected. FN-1 remains 0/49 shard and 0/3 miniature
under the default preset; those aggregate reports deliberately exit 1.

TS-34's independent naive evaluator agrees on the committed battery
(9 artifacts, 24 unused facts), ladder (5 compared, 0 facts, one plain-script
skip), 90 matrix artifacts, and an additional read-channel/unknown-syntax
witness. The five actual dispatch tests cover spans, suppression, defaults,
Unicode, split blocks, script-only files, Pug, read channels and uncertainty.
Static template refs account for the battery's 25 to 24 and ladder's 2 to 0
change; the positive battery and dedicated witness keep the nonempty gate.

TS-38's actual 130-file defused corpus lint with the rule enabled yields
zero mapped suppression candidates. The tooling gate checks all recall,
baseline and suppression invariants; miniature/identity/class tests pass.
The dedicated Actions workflow hydrates the pinned shard, reruns TS-34,
TS-35 (`fact_demand`), actual rule tests and the real CLI gates. Broad
workspace validation runs in the repository's regular Actions checks.

The five inventories, rule parity, assertion lint, tool module layout and
the no-new-or-grown-over-350-lines gate are refreshed/checked with this
change. The seed report fixture preserves FN-1 while closing ledger FN-2.
The built CLI regenerates all 398 explain pages in en/ja/zh (148 compiler
and other codes, 250 rules). Every prior page stays byte-identical; each
locale gains the new rule page and the strict catalog count is updated.
The localized rule metadata and exact tier census pass all 15 tooling tests.
Rust's registry census also pins 250 rules and one Sound Warning; only the
explicit `opt_in` preset snapshot gains the new rule.
The two script-parser Debug snapshots explicitly retain empty unused storage
under ordinary, undemanded parsing; all their existing fields are unchanged.
